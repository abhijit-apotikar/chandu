class TestStatistics {
  final double minMarks;
  final double maxMarks;
  final int toppersMarks;
  final int avgMarks;
  final int totalAppeared;
  final List<Map<String, dynamic>> topperList;

  TestStatistics(this.minMarks, this.maxMarks, this.toppersMarks, this.avgMarks,
      this.totalAppeared, this.topperList);
}
